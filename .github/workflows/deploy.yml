name: Deploy Django Staticfiles via FTP

on:
  push:
    branches:
      - master  # ou main, dependendo do seu repositório

jobs:
  deploy:
    runs-on: ubuntu-latest
    # ... resto do workflow ...

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'  # ou a versão que você usa

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Exporta SECRET_KEY para ambiente
        run: echo "SECRET_KEY=${{ secrets.SECRET_KEY }}" >> $GITHUB_ENV

      - name: Coletar arquivos estáticos
        run: python manage.py collectstatic --noinput

      - name: Deploy via FTP para Locaweb
        uses: locaweb/ftp-deploy@1.0.0
        with:
          host: ${{ secrets.HOST }}
          user: ${{ secrets.USER }}
          password: ${{ secrets.PASS }}
          localDir: "staticfiles"

      - name: Definir SECRET_KEY no servidor
        run: export SECRET_KEY='j6u&3sb-_hjl+58c0q_l)9$v91p%2pp4hd&1jynbyi8zf95&$u'